package commands

const (
	// Project top level command to manage projects
	Project string = "project"

	// Build top level command to manage builds
	Build string = "build"

	// Release top level command to manage releases
	Release string = "release"

	// Empty top level command to display default help
	Empty string = "empty"

	// ShHelp top level argument to display default help
	ShHelp string = "-h"

	// DhHelp top level command to display default help
	DhHelp string = "--help"
)
